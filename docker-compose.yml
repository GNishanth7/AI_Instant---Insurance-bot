services:
  api:
    build: .
    command: uvicorn backend.server:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
    environment:
      BACKEND_API_BASE_URL: http://api:8000
    depends_on:
      - api
    ports:
      - "3000:3000"
